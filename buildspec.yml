version: 0.1
phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 sync . "s3://${BUCKET_NAME}" --delete --exclude images/*
      - aws s3 sync ./images "s3://${BUCKET_NAME}/images" --delete --cache-control "max-age=31536000"
